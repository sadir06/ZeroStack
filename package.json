{
  "scripts": {
    "dev": "npx concurrently \"cd search_service && uvicorn main:app --reload --host 0.0.0.0 --port 8000\" \"cd zerostack-frontend && npm start\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.11",
    "tailwind": "^4.0.0"
  }
}
